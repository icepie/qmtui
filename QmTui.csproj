<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>qmtui</AssemblyName>
    <TargetFramework Condition="'$(RuntimeIdentifier)' != 'win-x64' and '$(RuntimeIdentifier)' != 'win-arm64' and ('$(RuntimeIdentifier)' != '' or '$(OS)' != 'Windows_NT')">net10.0</TargetFramework>
    <TargetFramework Condition="'$(RuntimeIdentifier)' == 'win-x64' or '$(RuntimeIdentifier)' == 'win-arm64' or ('$(RuntimeIdentifier)' == '' and '$(OS)' == 'Windows_NT')">net10.0-windows10.0.26100.0</TargetFramework>
    <EnableWindowsTargeting Condition="'$(RuntimeIdentifier)' == 'win-x64' or '$(RuntimeIdentifier)' == 'win-arm64' or ('$(RuntimeIdentifier)' == '' and '$(OS)' == 'Windows_NT')">true</EnableWindowsTargeting>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <NoWarn>$(NoWarn);CS0618</NoWarn>
    <PublishAot>true</PublishAot>
    <OptimizationPreference>Speed</OptimizationPreference>
    <IlcOptimizationPreference>Speed</IlcOptimizationPreference>
    <IlcInstructionSet Condition="'$(RuntimeIdentifier)' == 'linux-x64' or '$(RuntimeIdentifier)' == ''">x86-64-v3</IlcInstructionSet>
    <TieredCompilation>true</TieredCompilation>
    <TieredPGO>true</TieredPGO>
    <StripSymbols>true</StripSymbols>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ServerGarbageCollection>false</ServerGarbageCollection>
    <ConcurrentGarbageCollection>true</ConcurrentGarbageCollection>
    <IlcFoldIdenticalMethodBodies>true</IlcFoldIdenticalMethodBodies>
    <DefaultItemExcludes>$(DefaultItemExcludes);tui/**;tests/**;experimental/**;old/**;web/**;www/**;**/*.raw.cs</DefaultItemExcludes>
    <InvariantGlobalization>true</InvariantGlobalization>
    <LinkerFlavor Condition="'$(RuntimeIdentifier)' == 'linux-arm64'">lld</LinkerFlavor>
  </PropertyGroup>

  <Target Name="SyncAcousticPipeline" BeforeTargets="BeforeBuild" Condition="Exists('src/Services/AudioRecognition/AcousticFingerprintExtractor.raw.cs') And Exists('.bin/codegen_acoustic.py')">
    <Exec Command="python3 .bin/codegen_acoustic.py" />
  </Target>

  <ItemGroup>
    <PackageReference Include="Terminal.Gui" Version="2.5.0" />
    <PackageReference Include="z440.atl.core" Version="7.17.0" />
    <PackageReference Include="StbImageSharp" Version="2.30.16" />
    <PackageReference Include="StbImageWriteSharp" Version="1.16.7" />
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.12" />
    <PackageReference Include="ZXing.Net" Version="0.16.11" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="QmTui.Tests" />
  </ItemGroup>

  <!-- Collect generated files only after assembly, including on a clean checkout. -->
  <Target Name="BuildWebAssets" BeforeTargets="AssignTargetPaths" Condition="'$(DesignTimeBuild)' != 'true'">
    <Exec Command="node &quot;$(MSBuildProjectDirectory)/scripts/build-web.mjs&quot;" WorkingDirectory="$(MSBuildProjectDirectory)" />
    <ItemGroup>
      <EmbeddedResource Include="www/**" WithCulture="false" />
      <None Include="www/**" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
    </ItemGroup>
  </Target>

</Project>
